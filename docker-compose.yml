services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - 11434:11434
    environment:
      - OLLAMA_DEBUG=1
      - OLLAMA_KEEP_ALIVE=24h
      - OLLAMA_MAX_LOADED_MODELS=1
    volumes:
      - ai_evaluation_ollama:/root/.ollama
    networks:
      - ai_evaluation_network
    restart: always
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - 8080:8080
    environment:
      - ENV=dev
      - OLLAMA_BASE_URL=http://ollama:11434
    volumes:
      - ai_evaluation_open-webui:/app/backend/data
    depends_on:
      - ollama
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_evaluation_network
    restart: always

networks:
  ai_evaluation_network:
    driver: bridge

volumes:
  ai_evaluation_ollama:
  ai_evaluation_open-webui:
